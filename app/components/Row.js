/*import React from 'react';
import {
    View,
    Text,
    StyleSheet,
    Image,
    TouchableOpacity } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 12,
    flexDirection: 'row',
    alignItems: 'center',
  },
  text: {
    marginLeft: 12,
    fontSize: 17,
    color :'#000000',
  },
  photo: {
    height: 50,
    width: 50,
    borderRadius: 20,
  },
});

const Row = (props) => (

  <View >
   <TouchableOpacity style={styles.container}  onPress={() => console.log('load more')}>
      <Image source={{ uri: props.picture.large}} style={styles.photo} />
    <Text style={styles.text}>
      {`${props.name.first} ${props.name.last}`}
    </Text> 
    </TouchableOpacity>
  </View>
 
);

export default Row;*/